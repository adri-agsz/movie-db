/*import React from 'react';
import { Meta, Story } from '@storybook/react';
import Table from './Table';
import { ITable, ICall } from './types';
import { calls } from '../../components/Table/callsMock';

// Metadata para tu componente y las historias
const meta: Meta = {
  title: 'Components/Table',
  component: Table,
  parameters: {
    layout: "centered",
    docs: {
      storyDescription: "A Table component",
      iframeHeight: 400,
    }
  },
  tags: ["autodocs"]
};

// Exporta el metadata
export default meta;

// Define una función para el Template
const Template: Story<ITable> = (args) => <Table {...args} />;

// Define las historias
export const Default: Story<ITable> = Template.bind({});
Default.args = {
  data: calls // Aquí podrías pasar datos específicos para probar tu componente
};
*/

export default {}